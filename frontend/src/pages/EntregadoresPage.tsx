import { PagePlaceholder } from '../components/PagePlaceholder';

export function EntregadoresPage() {
  return (
    <PagePlaceholder
      title="Entregadores"
      description="Cadastro, consulta, edicao, disponibilidade e desativacao de entregadores."
      actionLabel="Novo entregador"
    />
  );
}

