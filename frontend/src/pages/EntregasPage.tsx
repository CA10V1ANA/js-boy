import { PagePlaceholder } from '../components/PagePlaceholder';

export function EntregasPage() {
  return (
    <PagePlaceholder
      title="Entregas"
      description="Criacao, designacao de entregador, status, historico e botao para rota no mapa."
      actionLabel="Nova entrega"
    />
  );
}

